// Import React from 'react'
import React from 'react';

// Define a function component called Loading
function Loading() {
  // Return JSX that renders a loading message
  return (
    <p>Loading...</p>
  );
}

// Export the Loading component very important
export default Loading;
